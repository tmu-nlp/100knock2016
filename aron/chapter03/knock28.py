# knock28.py
import sys
def plainText(left, right, content)
	for (i, ch) in enumerate(content):
		if ch == left:
			lst.append(i)
		elif ch == right:
			start = lst.pop()
			return content[start: i + 1]

